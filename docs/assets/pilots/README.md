Drop one square illustrated headshot per pilot here, named after the
pilot in lowercase: `marc.png`, `charles.png`. The game picks them up
automatically and shows them circle-cropped in the pilot's ship colour
everywhere faces appear. No file = the drawn avatar from the MY PILOT
editor is used instead.
